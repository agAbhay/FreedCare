
To run the app exec the cmd: npx expo start --clear

then simply scan the QR to run the app in your phone, but firs download Expo go app though which you can run this app locally on your phone

and to run on web exc cmd: npm run web

and you can test it on browser
